name: ci

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder tests
        run: |
          echo "CI is wired up."
          echo "Replace this step with real tests and lint later."
