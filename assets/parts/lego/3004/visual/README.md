# Visual Assets

`mesh.glb` is the optional high-resolution visual mesh for this part.

It is not committed yet. The project treats visuals as a separate layer from
lattice occupancy and connector semantics.
